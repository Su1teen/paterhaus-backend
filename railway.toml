[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
# Migrations run before the API starts; no destructive commands are executed.
startCommand = "npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
